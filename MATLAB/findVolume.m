%% findVolume
% This function takes in a point cloud and a cluster number and finds the
% volume of the coordinate box that contains the points.
function [volume] = findVolume(ptCloud, clusterNumber)
numberPoints = ptCloud.Count;

for 

end